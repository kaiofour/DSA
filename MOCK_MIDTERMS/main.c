#include <stdio.h>
#include <stdlib.h>
#include "mock.c"
#define MAX 10
#define SIZE 5

/**
 * @function_prototypes
 */
/**
 * 1.) Get all the voters living in the specified city
 * Source: PersonList
 * Destination: StackLList
 */

/**
 * 2.) Get the names of all voters with id 1 to 100 that are residing
 * in a a specified city
 * Source: PersonOpenDict
 * Destination: String array
 * - Add a sentinel using an empty string
 */

/**
 * 3.) Get all unique voters in list
 * Source: StackList
 * Destination: QueuePersonLList
 * - Best implemented in between with a Dictionary
 */

//Requirement functions for Number One
StackLList getVoters(PersonList vList, String city); //1. Add a sentinel using an empty string
//void getVoters(PersonList vList, StackLList *v, String city);

//2.
String *getNames(PersonOpenDict d, String city);

//3. for number 3, make your own function prototype based on the guidelines

/**
 * @main_function
 */
int main() {
    


    return 0;
}

StackLList getVoters(PersonList vList, String city){ //1. Add a sentinel using an empty string

} 
//void getVoters(PersonList vList, StackLList *v, String city);

String *getNames(PersonOpenDict d, String city) {//2
   
}